跳一跳 Python 版（与原 jump/ JS 版共存）

目录:
shortx.getShortXDir()/jump_python/

文件:
loader.js      ShortX入口/悬浮窗
jump.py        Termux Python识别核心
config.json    参数配置

依赖:
Termux 中已安装 python、numpy、pillow。

注意:
1. 不覆盖原来的 jump/。
2. 图片、结果、调试图都保存在 jump_python/。
3. ShortX 里运行 loader.js。
